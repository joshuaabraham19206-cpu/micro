name: micro
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.9.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # For making HTTP API calls (used in chat_screen.dart)
  http: ^1.2.1

  # For custom fonts (used in main.dart and home_screen.dart)
  google_fonts: ^6.2.1

  # iOS style icons
  cupertino_icons: ^1.0.8

  # For opening web links
  url_launcher: ^6.3.0

  # ▶️ YouTube video player (for video ID playback)
  youtube_player_flutter: ^9.0.3

  # 💾 Local storage for saving journal entries
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
